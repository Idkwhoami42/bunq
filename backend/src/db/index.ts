// import postgres from "postgres";

// export const sql = postgres({});

export const trips: {
    id: string;
    name: string;
    creator: string;
    location: string;
    users: string[];
}[] = [];
